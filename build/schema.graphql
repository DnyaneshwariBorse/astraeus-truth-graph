type TruthScore @entity(immutable: false) {
  id: ID!
  user: Bytes!
  score: BigInt!
  timestamp: BigInt!
}

type Verification @entity(immutable: false) {
  id: ID!
  queryHash: Bytes!
  score: BigInt!
  blockNumber: BigInt!
}
